$("#actual-pull-form").html("<%= escape_javascript(render 'pullform', pull: @pull, locals: {redirect_path: dorm_path(@dorm)}) %>");
var room_index = <%= @room_index %>;
$('.room-num-field').val(room_index);