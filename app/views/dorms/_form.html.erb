<%= form_with(model: dorm, local: true) do |form| %>
  <% if dorm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dorm.errors.count, "error") %> prohibited this dorm from being saved:</h2>

      <ul>
      <% dorm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dorm_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
