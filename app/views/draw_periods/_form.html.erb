<%= form_with(model: draw_period, local: true) do |form| %>
    <% if draw_period.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(draw_period.errors.count, "error") %> prohibited this draw period from being saved:</h2>

        <ul>
        <% draw_period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
        </ul>
    </div>
    <% end %>

    <% Time.use_zone(Time.zone) do %>
    <div class="field">
    <%= form.label :start %>
    <%= form.datetime_select :start, id: :start %>
    </div>

    <div class="field">
    <%= form.label :end %>
    <%= form.datetime_select :end, id: :end %>
    </div>
    <% end %>

    <div class="actions">
    <%= form.submit %>
    </div>
<% end %>